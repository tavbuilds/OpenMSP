<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    /**
     * No demo data. On first deploy the database is empty and the operator
     * creates the first administrator account via the setup screen (/admin).
     */
    public function run(): void
    {
        //
    }
}
